@model tl2_tp6_2024_s0a0m.Models.Presupuesto

@{
    ViewData["Title"] = "Crear Presupuesto";
}

<h1>@ViewData["Title"]</h1>

<form asp-action="CrearPresupuesto" method="post">
    <div class="form-group">
        <label for="NombreDestinatario">Nombre Destinatario</label>
        <input asp-for="NombreDestinatario" class="form-control" id="NombreDestinatario" required />
        <span asp-validation-for="NombreDestinatario" class="text-danger"></span>
    </div>

    @* <h3>Detalles del Presupuesto</h3>

    <div id="detallePresupuesto">
        <div class="form-group">
            <label for="Producto_0">Producto</label>
            <select name="Detalle[0].ProductoId" class="form-control" required>
                <option value="">Seleccionar Producto</option>
                @foreach (var producto in ViewData["Productos"] as List<tl2_tp6_2024_s0a0m.Models.Producto>)
                {
                    <option value="@producto.IdProducto">@producto.Descripcion - @String.Format("{0:C}", producto.Precio)</option>
                }
            </select>
            <span class="text-danger"></span>
        </div>

        <div class="form-group">
            <label for="Cantidad_0">Cantidad</label>
            <input type="number" name="Detalle[0].Cantidad" class="form-control" required min="1" />
            <span class="text-danger"></span>
        </div>
    </div>

    <button type="button" id="addProducto" class="btn btn-secondary mb-3">Agregar Producto</button> *@

    <button type="submit" class="btn btn-primary">Crear Presupuesto</button>
</form>